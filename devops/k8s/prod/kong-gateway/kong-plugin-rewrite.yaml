apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: rewrite-root-to-swagger
  namespace: gerador-uuid-prod
plugin: request-transformer
config:
  # Substitui a URI da requisição
  replace:
    uri: /swagger-ui/index.html